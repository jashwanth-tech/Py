name: Render Manim Animation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    container:
      image: manimcommunity/manim:latest
      options: --user root

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Fix git ownership
        run: git config --global --add safe.directory '*'

      - name: Render Video
        # RIGHT HERE: superluminal_laser.py is passed to the manim command
        run: manim -qh --resolution 1080,1920 superluminal_laser.py SuperluminalLaser

      - name: Upload Video Artifact
        uses: actions/upload-artifact@v4
        with:
          name: superluminal-laser-video
          # AND HERE: Manim creates a folder named 'superluminal_laser'
          path: media/videos/superluminal_laser/**/*.mp4
          retention-days: 7
